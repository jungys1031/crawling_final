<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTDMapper3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="giga.crawling_web.model.Crawling">

    <select id="crawlingList" resultType="giga.crawling_web.model.Crawling">
        SELECT * FROM crawling
        ORDER BY idx
    </select>

</mapper>
